name: Lighthouse
on: [push]

jobs:
  lighthouse:
  if: false
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Lighthouse
      uses: foo-software/lighthouse-check-action@master
      with:
        urls: 'https://www.nedko.info,https://www.transportapi.com'
